<div class="col-md-6 mt-3">
	<div class="form-group">
    <label class="form-label" for="[[field_name]]">[[field_label]]</label>

@php
    
    if(!Empty($row)){ 
        $selected = $row->[[field_name]]; 
    }else{
        $selected = '';
    }
    //$options = get_list_values($params->listmodulename, $params->listmoduleitem);
    //pre($options);
	$options = $[[field_name]];
 @endphp

		 <select class="form-control" name="[[field_name]]" id="[[field_name]]"> 
		   <option value="">--Select--</option>
		   @foreach ($options as $key => $value)
			<option value="{{ $options.id }}" {{ ( $options.id == $selected) ? 'selected' : '' }}> 
				{{ $options.[[field_name]] }} 
			</option>
		  @endforeach    
		</select>

		<input 
			type="hidden"
			id="h[[field_name]]"
			name="h[[field_name]]"
			value="{{ $row->[[field_name]] }}"
		/>
 </div>
 <div id="diverr_[[field_name]]" text-danger></div>
</div>