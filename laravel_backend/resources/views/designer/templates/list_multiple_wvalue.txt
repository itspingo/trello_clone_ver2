<div class="col-md-6 mt-3">
	<div class="form-group">
    <label class="form-label" for="[[field_name]]">[[field_label]]</label>
    
 @php
    
    if(!Empty($row)){ 
        $selected = explode(',',$row->[[field_name]]); 
    }else{
        $selected = array();
    }
    //$options = get_list_values($params->listmodulename, $params->listmoduleitem);
	$options = $[[list_name]];
 @endphp

<select class="form-control select2 select2-multiple" multiple="multiple" name="[[field_name]][]" id="[[field_name]]">
  <option value="">--Select--</option>
  @foreach ($options as $key => $value)
    <option value="{{ $options.id }}" 
        @if(in_array($options.id, $selected)) selected="selected" @endif 
    > 
        {{ $options.[[col_name]] }} 
    </option>
  @endforeach    
</select>

		<input
			type="hidden"
			id="h[[field_name]]"
			name="h[[field_name]]"
			value="{{ $row->[[field_name]] }}"
		/>
		
 </div>
 <div id="diverr_[[field_name]]" text-danger></div>
</div>