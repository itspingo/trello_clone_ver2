<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\card_buttons_model;


/* ******************************************************
    This file is generated by SNOOBIX on Jun 01, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Card_Buttons extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Card Buttons';
		$data['rows'] = card_buttons_model::orderBy('id', 'desc')->get();
		return view("backend/card_buttons_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Card Buttons';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Card Buttons';
       return view("backend/card_buttons_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Card Buttons';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = card_buttons_model::create($record); */

        $record = $request->all();
        
        
        
        $savedid = card_buttons_model::create($record)->id;
		if($savedid){
			 //return view("backend/card_buttons_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/card_buttons/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/card_buttons_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Card Buttons';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = card_buttons_model::where($recCond)->first();
		return view("backend/card_buttons_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Card Buttons';
        $id = $request->post('id');
        $id = str2id($id);
        $vcard_buttons_model = card_buttons_model::find($id);
        
         $record = $request->all();
         
        
        $isaved = $vcard_buttons_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/card_buttons_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/card_buttons')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = card_buttons_model::where($recCond)->first();
			return view("backend/card_buttons_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vcard_buttons_model = card_buttons_model::find($id);
        $isdeleted = $vcard_buttons_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.card_buttons')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.card_buttons')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
