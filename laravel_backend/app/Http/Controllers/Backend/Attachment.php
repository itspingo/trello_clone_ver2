<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\attachment_model;


/* ******************************************************
    This file is generated by SNOOBIX on Jun 01, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Attachment extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Attachment';
		$data['rows'] = attachment_model::orderBy('id', 'desc')->get();
		return view("backend/attachment_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Attachment';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Attachment';
       return view("backend/attachment_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Attachment';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = attachment_model::create($record); */

        $record = $request->all();
        
        
        
        $savedid = attachment_model::create($record)->id;
		if($savedid){
			 //return view("backend/attachment_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/attachment/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/attachment_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Attachment';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = attachment_model::where($recCond)->first();
		return view("backend/attachment_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Attachment';
        $id = $request->post('id');
        $id = str2id($id);
        $vattachment_model = attachment_model::find($id);
        
         $record = $request->all();
         
        
        $isaved = $vattachment_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/attachment_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/attachment')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = attachment_model::where($recCond)->first();
			return view("backend/attachment_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vattachment_model = attachment_model::find($id);
        $isdeleted = $vattachment_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.attachment')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.attachment')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
