<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\invitations_model;


/* ******************************************************
    This file is generated by SNOOBIX on Jun 01, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Invitations extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Invitations';
		$data['rows'] = invitations_model::orderBy('id', 'desc')->get();
		return view("backend/invitations_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Invitations';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Invitations';
       return view("backend/invitations_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Invitations';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = invitations_model::create($record); */

        $record = $request->all();
        
        
        
        $savedid = invitations_model::create($record)->id;
		if($savedid){
			 //return view("backend/invitations_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/invitations/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/invitations_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Invitations';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = invitations_model::where($recCond)->first();
		return view("backend/invitations_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Invitations';
        $id = $request->post('id');
        $id = str2id($id);
        $vinvitations_model = invitations_model::find($id);
        
         $record = $request->all();
         
        
        $isaved = $vinvitations_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/invitations_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/invitations')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = invitations_model::where($recCond)->first();
			return view("backend/invitations_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vinvitations_model = invitations_model::find($id);
        $isdeleted = $vinvitations_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.invitations')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.invitations')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
