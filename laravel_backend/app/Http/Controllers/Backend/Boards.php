<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\boards_model;


/* ******************************************************
    This file is generated by SNOOBIX on Jun 01, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Boards extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Boards';
		$data['rows'] = boards_model::orderBy('id', 'desc')->get();
		return view("backend/boards_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Boards';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Boards';
       return view("backend/boards_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Boards';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = boards_model::create($record); */

        $record = $request->all();
        if ($request->hasFile('background_image')) {
            $imgFile = $request->file('background_image')->store('public');
            $record['background_image'] = $imgFile;
        }
        if ($request->hasFile('logo')) {
            $imgFile = $request->file('logo')->store('public');
            $record['logo'] = $imgFile;
        }

        
        $record['member_ids'] = implode(',',$request->post('member_ids'));
        $savedid = boards_model::create($record)->id;
		if($savedid){
			 //return view("backend/boards_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/boards/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/boards_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Boards';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = boards_model::where($recCond)->first();
		return view("backend/boards_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Boards';
        $id = $request->post('id');
        $id = str2id($id);
        $vboards_model = boards_model::find($id);
        
         $record = $request->all();
         if ($request->hasFile('background_image')) {
$imgFile = $request->file('background_image')->store('public');
$record['background_image'] = $imgFile;
}
if ($request->hasFile('logo')) {
$imgFile = $request->file('logo')->store('public');
$record['logo'] = $imgFile;
}

        
        $isaved = $vboards_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/boards_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/boards')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = boards_model::where($recCond)->first();
			return view("backend/boards_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vboards_model = boards_model::find($id);
        $isdeleted = $vboards_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.boards')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.boards')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
