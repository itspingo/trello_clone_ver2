<?php

namespace App\Http\Controllers\Backend;
use App\Http\Controllers\Controller;
use Illuminate\Http\Request;

use App\Models\board_lists_model;


/* ******************************************************
    This file is generated by SNOOBIX on Jun 01, 2024
    Please visit snoobix.com for more details
****************************************************** */


class Board_Lists extends Controller
{
    public function __construct()
    {
        //if (!Auth::check()) return 'access_denied';
    }
   
    public function index()
    { 
        $data['module_title'] = 'Board Lists';
		$data['rows'] = board_lists_model::orderBy('id', 'desc')->get();
		return view("backend/board_lists_list", $data);
	
    }


    public function create()
    {
        $data['module_title'] = 'Board Lists';
       //$data['row'] = [];
       $data['submit_path'] = 'store';
       $data['module_name'] = 'Board Lists';
       return view("backend/board_lists_addedit", $data);       
    }

   
    public function store(Request $request)
    {
         $data['module_title'] = 'Board Lists';
        /* $request->validate([
            [[validateFieldsArray]]
        ]); */

        /* $record = [
			[[fieldsArray]]
		];
		$iscreated = board_lists_model::create($record); */

        $record = $request->all();
        if ($request->hasFile('head_image')) {
$imgFile = $request->file('head_image')->store('public');
$record['head_image'] = $imgFile;
}
if ($request->hasFile('background_image')) {
$imgFile = $request->file('background_image')->store('public');
$record['background_image'] = $imgFile;
}

        
        $record['member_ids'] = implode(',',$request->post('member_ids'));
        $savedid = board_lists_model::create($record)->id;
		if($savedid){
			 //return view("backend/board_lists_addedit")->with('flash_success', "Record is saved successfuly");
             return redirect('backend/board_lists/create')->with('flash_success', "Record is saved successfuly");
		}else{
			$data['row'] = $request->all();
			return view("backend/board_lists_addedit", $data)->with('flash_failure', "Record is NOT saved successfuly");
		}
    }

    
    public function show()
    {
        //
    }

    
    public function edit($id)
    {
         $data['module_title'] = 'Board Lists';
        $data['submit_path'] = 'update';
        $recCond = [
			'id' => str2id($id)
		];
		$data['row'] = board_lists_model::where($recCond)->first();
		return view("backend/board_lists_addedit", $data);
    }

   
    public function update(Request $request)
    {
		 $data['module_title'] = 'Board Lists';
        $id = $request->post('id');
        $id = str2id($id);
        $vboard_lists_model = board_lists_model::find($id);
        
         $record = $request->all();
         if ($request->hasFile('head_image')) {
$imgFile = $request->file('head_image')->store('public');
$record['head_image'] = $imgFile;
}
if ($request->hasFile('background_image')) {
$imgFile = $request->file('background_image')->store('public');
$record['background_image'] = $imgFile;
}

        
        $isaved = $vboard_lists_model->fill($record)->save();  //this will save record with all fillable items

        if($isaved){
			//return view("backend/board_lists_addedit")->with('flash_success', "Record is updated successfuly");
            return redirect('backend/board_lists')->with('flash_success', "Record is updated successfuly");
		}else{

			//$data['postData'] = $record;
             $data['submit_path'] = 'update';
             $recCond = [
                'id' => $id,
            ];
            $data['row'] = board_lists_model::where($recCond)->first();
			return view("backend/board_lists_addedit", $data)->with('flash_failure', "Record is NOT updated successfuly");
		}		
    }

   
    public function destroy(Request $request)
    {
        $id = $request->post('delid');
        $id = str2id($id);
        $vboard_lists_model = board_lists_model::find($id);
        $isdeleted = $vboard_lists_model->delete();
        if($isdeleted ){
            return redirect()->route('backend.board_lists')->with('flash_success','Record is deleted successfuly');
        }else{
            return redirect()->route('backend.board_lists')->with('flash_failure','Record is not deleted, try again later');
        }
    }
}
